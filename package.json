{
  "name": "google-spreadsheet-writer",
  "version": "v1.0.0-alpha.0",
  "description": "A Google Apps Script project for writing styled JSON data to Google Sheets",
  "main": "Code.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "docs:generate": "npx documentation build src/** -f md -o docs/docs/doc.md && node scripts/clean-md.js",
    "docs:dev": "npm --prefix docs run start",
    "docs:build": "npm run docs:generate && npm --prefix docs run build",
    "docs:serve": "npm --prefix docs run serve"
  },
  "keywords": [],
  "author": "Yeifer <developer@yeifer.co>",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^30.1.2",
    "documentation": "^14.0.3",
    "jest": "^30.1.3"
  }
}
